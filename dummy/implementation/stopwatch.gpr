project Stopwatch is
   for Source_Dirs use ("src");
   for Main use ("stopwatch.adb");
   for Object_Dir use "obj";
   package Compiler is
      for Default_Switches ("ada") use ("-gnat12", "-gnatd.V", "-gnatdk");
   end Compiler;
end Stopwatch;
